# Implementation Plans

Plans 012-026 were generated by the `improve` skill on 2026-07-29 against commit `fabeb49`; Plan 027 was added against commit `7eebe14`. Execute in the order below unless dependencies say otherwise. Every executor must read the whole selected plan, run its drift check, honor STOP conditions, run every verification gate, and update the status row.

Plans 028-033 were generated by the `improve` skill on 2026-07-30 against
commit `cef0acd` after the project owner selected all six vetted direction
findings. This batch converts the completed product designs into deliberately
sliced work: pure Analyzer core, governed validation, assessment coverage,
privacy-preserving measurement, production Analyzer UX, and a three-page
crawlable content pilot. The plans are self-contained; executors must not infer
that approval of one plan authorizes later dependent plans or external actions.

Plan 034 was added on 2026-07-30 against the same commit after the owner
identified SoloTodo as a strategically important Chilean price-comparison
source. It is a permission and feasibility qualification, not authorization to
scrape, contact an external party, publish provider data, or ship production
price comparison.

Plan 035 was added on 2026-07-31 after the owner rejected Plan 029's recurring
independent-expert labeling model as infeasible for a solo, free product. It
implements the canonical replacement: automated, implementation-independent
rule conformance and critical negative controls, separated from a private
unlabeled real-quote coverage corpus. No AI output is reclassified as expert
ground truth.

Plans 037-051 were generated by the `improve` skill on 2026-09-25 against
commit `6cde4b8` after the project owner selected every net-positive finding
from that session's deep audit. The batch groups into: verification and
toolchain hygiene (037, 046), truthful power/connector outcomes (038), analyzer
quote-quality defects (039), App characterization tests and dead-code
retirement (040, 041), resolver performance (042), analyzer context and
degraded-data surfacing (043), data-lifecycle hardening (044), documentation
truth-up (045), pipeline symlink rejection (047), and four design/decision
spikes (048-051). Plans 048-051 are design-only: they authorize no
implementation, no deployment, and no external submission. Approval of one
plan never authorizes a dependent plan.

> **Execution view**: see [ROADMAP.md](ROADMAP.md) for the milestone
> scoreboard, wave scheduling (wall-clock grouping and parallelism rules), the
> file-conflict map, the owner decision queue, and the executor protocol.

## Product-governance requirement

Before approving or implementing any material plan, read [the Canonical Product Vision](../docs/PRODUCT_VISION.md). It governs product direction above plans and current code. Plans that change recommendations, compatibility, core flows, price/assessment meaning, unknown/warning/failure semantics, ranking, or constraining architecture include a product-decision record; do not remove or bypass it. Surface a conflict and request an explicit owner decision rather than silently overriding the vision.

The 2026-07-30 vision amendment establishes outcome-based milestones and an explicit current sequence:

- Plan 024, the Quote Analyzer design, contributes to Milestones 0 and 2 and is the first product-direction vertical.
- The existing manual builder remains a supporting Expert Builder surface.
- The Guided Builder portion of Plan 025 must not become a production implementation before the shared analyzer assessment contract and Milestone 2 quality gates exist.
- Organic-search foundation work contributes to Milestone 1, but indexed content must provide unique decision value and must not become a thin programmatic-content project.
- Analyzer public enablement depends on Plan 035's bounded automated assurance,
  not Plan 029 expert agreement. Gaming balance remains unsupported.
- Monetization work is out of current scope until Milestone 4 is achieved and the project owner explicitly authorizes Milestone 5.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---:|---:|---|---|
| [016](archive/016-extract-catalog-compiler.md) | Extract a pure, fixture-tested catalog compiler | P1 | L | 009, 011 | DONE |
| [017](archive/017-correct-case-form-factors.md) | Separate case chassis class from motherboard compatibility | P1 | M | 016 | DONE |
| [018](archive/018-guarantee-catalog-id-uniqueness.md) | Guarantee unique catalog IDs without orphaning saved quotes | P1 | L | 015, 016 | DONE |
| [020](archive/020-restore-mobile-core-actions.md) | Keep core quote actions available on mobile | P2 | M | 012 | DONE |
| [021](archive/021-reconcile-onboarding-and-product-docs.md) | Reconcile contributor, maintenance, product-state, and plan docs | P2 | M | 009, 010, 011 | DONE |
| 022 | Decompose App into tested domain and UI boundaries | P2 | L | 013, 014, 015, 019, 020 | DONE |
| [027](027-adopt-typescript-7-incrementally.md) | Adopt TypeScript 7 incrementally without a big-bang rewrite | P2 | L | 017, 018, 022 | BLOCKED: stable typescript-eslint does not yet support TS 7.x directly |
| [023](archive/023-profile-and-stabilize-typeahead.md) | Profile and conditionally stabilize typeahead rendering | P3 | S-M | 022 | DONE |
| [024](archive/024-design-quote-analyzer.md) | Design imported-quote analysis as the first decision-engine vertical | P2 | L | 014, 015 | DONE |
| [025](archive/025-design-guided-and-expert-builders.md) | Define separate Guided and Expert Builder experiences | P2 | L | 014, 019 | DONE |
| [026](archive/026-design-scenario-comparison.md) | Design multi-quote comparison and upgrade scenarios | P3 | M | 013, 014, 024 | DONE |
| [028](archive/028-implement-quote-analyzer-core.md) | Implement the pure Quote Analyzer v1 core | P1 | L | 014, 015, 018, 024 | DONE |
| [029](029-establish-analyzer-validation-corpus.md) | Establish the Quote Analyzer validation corpus and offline harness | P1 | L | 028 | REJECTED: independent expert labeling is infeasible; superseded by 035 |
| [030](archive/030-generate-assessment-coverage-contract.md) | Generate the assessment coverage and evidence contract | P1 | M | 028 | DONE — manifest `assessment-coverage/v1` generated from the catalog snapshot; evidence preserved at mapping; archive on merge |
| [031](archive/031-define-decision-funnel-measurement.md) | Define the privacy-preserving decision-funnel measurement contract | P1 | M | 028 | DONE — frozen `decision-measurement/event/v1` contract, privacy envelope, and provider-neutral no-op/injected adapter; no network/storage wiring |
| [032](archive/032-ship-confirmation-driven-analyzer-ui.md) | Ship the confirmation-driven Quote Analyzer workflow | P1 | L | 028, 030, 031, 035 | DONE — Analyzer is the default workspace; owner-approved waiver on the Plan 035 real-input gate (no assurance claim); archive on merge |
| [033](033-build-crawlable-decision-content-foundation.md) | Build the crawlable Spanish decision-content foundation | P2 | L | 030, 031, 032, 035 | TODO |
| [034](034-qualify-solotodo-price-intelligence-source.md) | Qualify SoloTodo as a permissioned Chilean price-intelligence source | P1 | L | 030, 035 for evaluation; qualification may start now | TODO |
| [035](archive/035-automate-analyzer-assurance.md) | Replace expert labeling with automated Analyzer assurance | P1 | L | 028; 030 before final scoring | DONE |
| [036](archive/036-ingest-buildcores-evidence.md) | Ingest BuildCores evidence to unlock Analyzer rule coverage | P1 | L | 030, 032 | DONE — six BuildCores categories ingested; 4 rules unlocked, headroom 3.3%→50.8%; `mobo.max_memory_speed_mts` remains a documented gap |
| [037](037-add-per-push-verification-gate.md) | Add a per-push verification gate, clear toolchain advisories, wire the assurance CLI | P1 | S-M | — | DONE — reviewed 2026-09-25 (commits `9a5fc9a`…`97e8760`); merged to `advisor/b1-cotiza-verify` as `c36ee30`; `npm audit` 0 vulnerabilities, conformance 44/44 |
| [038](038-truthful-psu-and-connector-outcomes.md) | Report power and connector outcomes truthfully when data is missing | P1 | M | 037 | DONE — reviewed 2026-09-25 (commits `994e39a`, `6b1b882`); merged to `advisor/b1-cotiza-verify` as `05e9ca8`; missing TDP → unknown, 6/16-pin parsed, fabricated `recommended_psu_w` gone from 3,552 records |
| [039](039-quote-quality-defects.md) | Fix analyzer quote-quality defects (blank rows, price CSV, inferred evidence, signature) | P1 | S | 038 | DONE — reviewed 2026-09-25 (commit `d066a91`); merged to `advisor/b1-cotiza-verify` as `ca11b8e`; blank rows ignored, exact price-CSV id column, inferred case FF → medium, signature includes currency/priceUpdatedAt; assurance 44/44 |
| [040](040-characterize-app-quote-flows.md) | Characterize App quote CRUD, cascades, and export boundaries | P1 | M | — | DONE — reviewed 2026-09-25 (commit `e3c9e53`); merged to `advisor/b1-cotiza-verify` as `5d13de7`; App suite 121 passed / 1 todo (retained `it.todo` documents an unreachable alert branch: delete button is disabled for the last quote); real `escapeCsvField` in export tests |
| [041](041-retire-dead-builder-reducer.md) | Retire the unused builder reducer API | P2 | S | 040 | DONE — reviewed 2026-09-25 (commit `b49f7a9`); merged to `advisor/b1-cotiza-verify` as `7d562f7`; module renamed to `builderHelpers.js`, dead reducer API gone, helper tests retained/added |
| [042](042-resolver-performance.md) | Make analyzer row resolution cheap, bounded, and lazy | P1 | M | — | DONE — reviewed 2026-09-25 (commits `6bff8c9`…`b184520`); merged to `advisor/b1-cotiza-verify` as `a3e88ab`; O(1) id index, 20-candidate cap + manual search, `active` laziness, raw parse no longer retained; resolution parity tests + assurance 44/44 |
| [043](043-analyzer-context-and-failure-surfacing.md) | Allow context edits after analysis; surface degraded catalog data | P1 | S | 042 | DONE — reviewed 2026-09-25 (commit `90ba921`); merged to `advisor/b1-cotiza-verify` as `0de4e23`; "Editar contexto" reopens intake preserving mappings, `compatFailed` and coverage-unavailable hints surfaced without changing verdicts |
| [044](044-harden-import-persistence-workspace.md) | Harden persistence, imports, export filenames, mapper, history | P2 | S-M | 042, 043 | DONE — reviewed 2026-09-25 (commit `67acba0`); merged to `advisor/b1-cotiza-verify` as `b5d7381`; per-entry recovery with backup + StrictMode-safe first-write skip, unified import detection, slugified filenames, mapper guard, single `pushState` |
| [045](045-docs-truth-up.md) | Reconcile design/validation docs with shipped state | P2 | S | — | DONE — reviewed 2026-09-25 (commit `ea37c9b`); merged to `advisor/b1-cotiza-verify` as `ca0ed6c`; measurement/validation status corrected, example report marked synthetic, shipped facts and code refs updated; one justified extra file (`measurement-enabling-decision.md`) had two now-false claims corrected |
| [046](046-lint-scripts-and-asset-hygiene.md) | Lint the pipeline, strict app lint, prune stale deployed assets | P2 | S-M | 037 | DONE — reviewed 2026-09-25 (commit `3e80934`); merged to `advisor/b1-cotiza-verify` as `93dd68d`; scripts linted (16 dead bindings removed), `--max-warnings 0`, workflow gates, 5 stale bundles pruned; plan amended to fix the ESLint 10 base-path invocation |
| [047](047-pipeline-symlink-rejection.md) | Reject symlinked dataset files in pipeline readers | P2 | S | — | DONE — reviewed 2026-09-25 (commit `52e63dd`); merged to `advisor/b1-cotiza-verify` as `4c4e349`; guard fail-closed in every dataset read path; 5 new io tests |
| [048](048-coverage-case-contribution-design.md) | Design a privacy-reviewed coverage-case contribution path | P2 | S | — | DONE — reviewed 2026-09-25 (commit `fe1053a`); merged to `advisor/b1-cotiza-verify` as `f5fcf04`; owner checklist resolved 2026-09-25 (corpus adopt, trust-first); implementation shipped as 052 |
| [049](049-content-inventory-pages-4-12.md) | Author the pages 4-12 rule-backed content inventory | P2 | S | — | DONE — reviewed 2026-09-25 (commit `5bfbc10`); merged to `advisor/b1-cotiza-verify` as `0236a7a`; 9 rows, all rule IDs verified against the registry, page 5 deferred (coverage absent), design-only — authorizes no pages, deploy, or submission |
| [050](050-non-destructive-conflict-notices.md) | Announce builder clears caused by conflicts | P2 | S | 040 | BLOCKED — STOP on missing-data conflation: empty CPU socket / empty case `formFactors` satisfy the clear conditions, so notices would claim a mismatch where data is missing (120 shipped cases have empty form factors; 549 CPUs lack a socket). Changing clear conditions is out of scope; WIP preserved on `advisor/041-050-builder-cleanups` (`4e82726`); owner decision needed |
| [051](051-measurement-enabling-decision.md) | Produce the measurement-enabling decision packet | P2 | S | — | DONE — reviewed 2026-09-25 (commit `78669d5`); merged to `advisor/b1-cotiza-verify` as `12a900b`; no sink or collection authorized; measurement stays no-op until M1 traffic and owner re-approval |
| [052](052-coverage-case-export.md) | Add the local, privacy-minimized coverage-case export (corpus flywheel, layer 1) | P1 | M | 048, 037 | DONE — reviewed 2026-09-25 against real validator (commits `021d9b8`, `081af99`); merged to `advisor/b1-cotiza-verify` as `4ff321d`; one revision (retain user-confirmed rows); no network, no event |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency graph and execution notes

```text
009 ─┬─ 010 ──────────────┐
     ├─ 011 ── 016 ─┬─ 017│
     │               └─ 018
      └─ 012 ─┬─ 013 ──────────────┐
                ├─ 014 ─┬─ 019 ─┐   │
                │       ├─ 024 ─DONE─┼─ 026 ─DONE
                │       └─ 025 ─DONE │
                 ├─ 015 ──────┐  │
                └─ 020 ─DONE─┤  │
                             └──┴─ 022 ── 023 ─DONE─

009 + 010 + 011 ── 021 ─DONE─

017 + 018 + 022 ── 027

024 ── 028 ─┬─ 029 ─REJECTED
            ├─ 030 ────────────────┐
            ├─ 031 ────────────────┤
            └─ 035 ────────────────┤
                                    └─ 032 ─DONE─ 033

030 + 035 ── 034 qualification/evaluation ── future approved price integration
```

- Plan 010 combines four inseparable delivery findings: stale deployed catalogs, mutating verification, job-wide Pages/OIDC credentials, and mutable action tags. Splitting them into separate workflow edits would cause repeated, conflicting rewrites.
- Plan 012 is characterization-only and precedes every risky App behavior change.
- Plans 013-015 are independent after 012 and may run in parallel in separate branches/worktrees; reconcile `App.jsx` carefully before merging.
- Plan 016 creates the pure compiler seam required to fix semantic catalog data safely. Plan 017 may then change form-factor meaning; Plan 018 additionally depends on interchange identity semantics from 015.
- Plan 019 is the corrective completion of archived Plan 008. It depends on the assessment taxonomy from 014 so partial/fallback readiness cannot masquerade as valid.
- Plan 022 is deliberately late: do not refactor `App` while its money, assessment, interchange, staged-loading, and mobile behavior are still moving.
- Plan 027 follows catalog semantic fixes and App decomposition to avoid converting unstable contracts twice. Its first gate requires official stable typescript-eslint support for both TypeScript 7.x and ESLint 10 without a TypeScript 6 compatibility API. TypeScript 7.0 ships without the compiler API used by tools such as typescript-eslint, so direct support may require TypeScript 7.1 or later. The 2026-07-30 preflight evidence remains recorded in the plan. A side-by-side TypeScript 7 compiler and TypeScript 6 API arrangement requires an explicit owner-approved plan amendment; do not introduce it as an implementation workaround.
- Plan 023 was conditional: profiling stayed below its 50 ms threshold (max one frame, 0/14 frames over 50 ms), so simple stabilization landed and indexing/virtualization were not justified. Measurement retained in the archived plan.
- Plan 024 delivered `docs/design/quote-analyzer.md` (2026-07-30). Plans 025-026 remain design/data spikes. Design spikes do not authorize full feature builds; approved designs should generate new, sliced implementation plans. The owner approved design §12.1 (required-component set) on 2026-07-31, and Plan 028 implemented the approved defaults.
- Plan 025 delivered `docs/design/builder-modes.md` (2026-07-30). The existing picker is confirmed as the Expert Builder baseline; Guided intake and recommendation contracts are designed but not encoded. Budget/value rules stay preference markers until a licensed price feed exists (catalog verified to carry no prices). Phase A (Expert evidence/alternatives/non-destructive conflicts) is production-safe; the Guided engine (Phase B+) stays gated on the Plan 024 analyzer contract in production + Milestone 2 quality gates. Owner decisions in design §8 (persona scope, budget semantics, performance-band mapping, guided timing, silent-clear replacement).
- Plan 026 delivered `docs/design/scenario-comparison.md` (2026-07-30). Comparison is a consumer of the analyzer contract (`output/v1`), never a second engine; `hasWinner` is constant false in v1; acquisition cost excludes owned rows but keeps them in assessment; missing prices are never zero; mixed currencies mark price dimensions incomparable (no conversion — owner decision §9.1). Phase A (existing-engine quote diff/cost) is production-safe; Phase B+ gated on the Plan 024 analyzer in production. Owner decisions in design §9.
- Plan 028 is the first implementation slice from the approved Analyzer design.
  It owns the pure resolver/assembly/report contract and no UI. Its executor must
  obtain explicit approval of the v1 defaults recorded in the plan before code.
- Plan 029 is rejected and retained only as the historical reviewer-based
  method. Do not execute its remaining collection/adjudication steps or use its
  report as a launch gate.
- Plan 035 replaces Plan 029 with a committed conformance suite, critical
  negative controls, and a private unlabeled coverage corpus. It may begin from
  Plan 028's stable output; final scoring also consumes Plan 030 evidence
  coverage. Raw quotations never belong in git.
- Plan 030 initially reports rule-level assessability; it does not hard-fail a
  refresh for low coverage. Owner-approved thresholds may be introduced only
  after Plan 035 establishes automated conformance and coverage baselines. At
  batch creation, unrelated
  uncommitted `catalogMapper*` work overlapped its future scope; its executor
  must reconcile ownership and explicit/inferred semantics rather than
  overwriting those changes.
- Plan 031 defines event semantics and a provider-neutral no-op/injected adapter.
  It does not authorize a telemetry vendor, network sink, cookie, or raw quote
  data collection.
- Plan 032 shipped the production Analyzer UI and the honest Expert/Manual
  Builder label. The Plan 035 real-input coverage gate is not yet evaluable
  (no private corpus), so the project owner recorded an explicit waiver on
  2026-09-25: public enablement uses bounded-assurance copy only, missing or
  conflicting evidence still resolves to `unknown`, and the coverage gate is a
  later validation item rather than a launch blocker. See the archived plan for
  the full record.
- Plan 036 shipped catalog evidence coverage after Plan 032 shipped the
  Analyzer. It merged only exact BuildCores identity matches and included
  unmatched BuildCores records as separate source-attributed entries (owner
  decision 2026-09-25); no fuzzy spec assignment occurred. Measured results:
  cpu↔mobo socket 0→56.3%, cpu↔RAM 0→49.7%, gpu↔case length 0→44.4%,
  psu↔gpu connectors 0→17.0%, PSU headroom 3.3→50.8%, mobo↔case FF
  97.2→98.5%; `mobo.max_memory_speed_mts` has no source and remains a
  documented gap. It changed no rule semantics and makes no assurance claim;
  the Plan 035 real-input gate remains a later validation item. Future pin
  refreshes must re-run the before/after coverage comparison.
- Plan 033 is a three-page authored pilot, not the full twelve-page Milestone 1
  target and not programmatic SEO. External Search Console, sitemap submission,
  production build, and deployment remain separate explicitly authorized
  actions.
- Plan 034 may begin its public-evidence and owner-led outreach preparation in
  parallel. Its correctness evaluation consumes Plan 035 source-backed
  conformance fixtures while real-input coverage consumes Plans 030 and 035;
  any eventual
  Analyzer price evidence consumes Plan 028's stable contract. SoloTodo is a
  candidate primary Chilean offer-discovery provider, not a specification or
  compatibility authority. No scraping, external contact, API use, credentials,
  public data redistribution, or production price UI is authorized by the plan.
  A written permission matrix and explicit go/no-go decision precede a separate
  production implementation plan.
- Plan 027 is not a dependency for Plans 028-034. Use JSDoc contracts while the
  TypeScript 7 ecosystem gate remains blocked; do not introduce a side-by-side
  compiler workaround.

### 2026-09-25 M0 owner decisions (delegated to the advisor, trust-first)

The owner delegated judgment for M0. Decisions recorded, conservative by default:

- **Corpus (048) — ADOPT.** Implement the local `coverage-case/v1` download.
  Keep real `itemId` (hashing breaks the `exact-id` metric); v1 limited to
  `exact-id`/`user-mapped`, free text dropped; staging files deleted after each
  aggregate run, aggregates only persist; withdrawal by opaque `caseId`; the
  product never uploads (no endpoint, fetch, queue, or event). Implementation
  is Plan 052 — shipped and merged 2026-09-25 (`4ff321d`).
- **Measurement (051) — STAY NO-OP for now.** There is no indexed traffic
  (M1 is 0/12 pages), so a sink would collect almost nothing. Acquisition-class
  derivation (051 Rule C) may be built locally with the no-op sink to prove the
  classification, but **no collection is authorized** until M1 traffic exists
  and the owner re-approves. No implementation plan yet.
- **Consequence**: the corpus, not telemetry, is the first moat layer. Plan 052
  is therefore the highest-leverage next slice.

### 2026-09-25 batch 1 execution record (`advisor/b1-cotiza-verify`)

The owner authorized execution ("Do it"). Approved plan branches were merged
into the integration branch and the moat slice was executed and reviewed:

| Plan | Verdict | Commits | Merge |
|---|---|---|---|
| 037 per-push gate | APPROVE | `9a5fc9a`…`97e8760` | `c36ee30` |
| 048 corpus design | APPROVE | `fe1053a` | `f5fcf04` |
| 051 measurement packet | APPROVE | `78669d5` | `12a900b` |
| 052 corpus export | APPROVE after one revision | `021d9b8`, `081af99` | `4ff321d` |
| 038 truthful PSU/connectors | APPROVE | `994e39a`, `6b1b882` | `05e9ca8` |
| 047 symlink rejection | APPROVE | `52e63dd` | `4c4e349` |
| 039 quote-quality defects | APPROVE | `d066a91` | `ca11b8e` |
| 040 App characterization | APPROVE (1 retained todo) | `e3c9e53` | `5d13de7` |
| 042 resolver performance | APPROVE | `6bff8c9`…`b184520` | `a3e88ab` |
| 043 context + degraded data | APPROVE | `90ba921` | `0de4e23` |
| 044 persistence/import hardening | APPROVE | `67acba0` | `b5d7381` |
| 041 retire builder reducer | APPROVE | `b49f7a9` | `7d562f7` |
| 050 conflict notices | STOPPED (BLOCKED) | WIP `4e82726` | not merged |
| 046 lint + asset hygiene | APPROVE (plan amended for ESLint 10 base path) | `3e80934` | `93dd68d` |
| 049 pages 4-12 inventory | APPROVE | `5bfbc10` | `0236a7a` |
| 045 docs truth-up | APPROVE (one justified extra doc) | `ea37c9b` | `ca0ed6c` |

- 052's review caught a real corpus-killing defect: a user-confirmed row keeps
  its original (usually empty) `itemId`, so the first builder dropped exactly
  the rows Milestone 2 counts. The revision maps confirmed rows through
  `explicitMappings` and keeps the real `itemId`; independently reproduced
  before and after the fix.
- 038's review confirmed the compiler diff is exactly `recommended_psu_w` on the
  3,552 null-TDP records, no other field changed, and the analyzer's
  `hasTdp` guard in `report.js:348` still prevents null-envelope arithmetic.
  Recorded judgment call: the raw string `"None"` is now `unknown` (upstream
  `sanitizeConnectorString` maps it to missing data and no catalog record
  contains it).
- 047 merged (`4c4e349`): `assertNotSymlink` is fail-closed and outside the
  per-file parse catch, so a symlinked dataset aborts the scheduled build
  instead of being skipped.
- 039's review confirmed the four fixes are minimal and the inferred-confidence
  change is label-only (severity/status unchanged; assurance 44/44, zero
  critical false negatives).
- 040's review confirmed no production file was touched and the retained
  `it.todo` is honest: `App.jsx` disables delete for the last quote, so the
  `window.alert` branch in `handleDeleteQuote` is unreachable without a
  production change; the reachable guard is characterized instead.
- 042's review confirmed resolution-state parity via index/no-index deep-equal
  tests over every fixture plus assurance 44/44; the candidate cap is a UI
  bound paired with a manual `TypeaheadSelect` fallback; the raw parsed JSON
  is no longer retained (`noCache`), while `pending` dedupe is unchanged.
- 043's review confirmed the edit affordance sits outside the disabled
  fieldset, `compatFailed` stays separate from `fallbackUsed`, and the
  coverage hint does not block or alter the verdict. Recorded deviation: after
  "Editar contexto" the stale banner is unreachable (the verdict is unmounted
  at intake); the test asserts the re-analysis becomes current instead.
- 044's review confirmed the storage-safety goal. Recorded deviation: the
  plan's boolean one-write skip was replaced by an identity-based skip
  (`skipPersistRef` holds the hydrated array) because React StrictMode's
  double-invoked mount effect would otherwise overwrite the corrupt blob on
  the second pass; the executor proved the failure and the tests now cover
  StrictMode explicitly.
- 050's executor correctly hit its STOP condition and the reviewer verified it
  against the shipped catalog: `mobo.socket !== cpu.socket` is true when the
  CPU socket is missing, and `!currentCase.formFactors?.includes(...)` is true
  when a case has no form-factor data, so the planned notices would emit false
  compatibility claims. The clears themselves are pre-existing behavior and
  out of 050's scope; the full WIP (five notices, seven tests, StrictMode
  coverage) is preserved at `4e82726` unmerged. Owner decision required —
  see ROADMAP §7.
- 046's first run correctly stopped on a plan-authoring error: under ESLint 10
  `--config` makes the lint base path the process cwd, so the planned
  `eslint --config ../eslint.config.js ../scripts` can never lint the
  pipeline. The reviewer amended the plan to
  `cd .. && eslint --config eslint.config.js scripts` and resumed the same
  executor; it then removed exactly the 16 unused bindings (two renamed to
  `_`-prefixed bindings to preserve rest-sibling semantics), made app lint
  warning-free (`--max-warnings 0`), and pruned 5 stale bundles
  (`docs/assets` now equals the `docs/index.html` set).
- 049's inventory defers page 5 (RAM↔motherboard speed) because
  `compat-mobo-ram-memory` coverage is absent (0%); the document states its
  thresholds and authorizes no page work.
- 045's review confirmed the doc corrections and accepted one justified extra
  file: `docs/design/measurement-enabling-decision.md` (created by 051 after
  045 was planned) carried two "not yet instrumented" claims that Step 1 made
  false; the sweep's own done criterion requires removing them.
- Integration branch post-merge: `npm run check` exits 0 (0 errors, 0
  warnings, 44 test files, 1057 passed / 1 todo), `npm run test:assurance`
  passes, `verify.sh` passes.
- Batch state: Waves 0-5 complete (037-049, 051, 052). 050 is BLOCKED on the
  owner decision in ROADMAP §7; 033/034 and the page batches remain
  decision-gated. The plans docs and roadmap are committed on
  `advisor/b1-cotiza-verify`.

### 2026-09-25 batch notes

- Plan 037 should land first: it adds the per-push gate that verifies every
  later plan on an unmodified baseline, refreshes the lockfile to clear two
  high advisories, and makes the Plan 035 assurance CLI a documented,
  CI-enforced command.
- Plans 038 and 039 both touch analyzer-adjacent code and the assurance suite;
  run 038 before 039 so an assurance diff points at one change.
- Plans 042 → 043 → 044 share `QuoteAnalyzer.jsx`/`App.jsx`. Run them serially
  and reconcile; plan 040 must land before 041 and 050.
- Plan 046 depends on 037 for the workflow/script surfaces and on 042/043 for
  `QuoteAnalyzer.jsx`. Plan 045 should run after behavior-changing plans so its
  current-state corrections reflect reality.
- Plans 048-051 are design/decision spikes. They produce documents only;
  implementation requires a new owner-authorized plan, and 048/051 additionally
  require explicit owner decisions on privacy/retention before any code.

## Audit finding coverage

Every net-positive finding from the 2026-07-29 deep audit and subsequent
direction reviews is assigned:

| Audit finding | Implementation plan |
|---|---|
| Refreshed catalog is validated but stale `docs/` is uploaded | 010 |
| Custom currency crashes on intermediate input | 013 |
| Locale-formatted prices and partial savings produce wrong totals | 013 |
| Zero-match price imports falsely refresh prices | 013 |
| Unknown/warning assessment can render “Todo ok”; RAM warning is dropped | 014 |
| JSON import duplicates local IDs; CSV drops catalog `itemId` | 015 |
| CSV formula injection on spreadsheet open | 015 |
| Case chassis types are misused as motherboard form factors | 016, 017 |
| Catalog IDs collide across products | 016, 018 |
| App core workflows have only shallow integration coverage | 012 |
| Downloader tests mutate ignored production-like raw provenance | 011 |
| Catalog compiler lacks pure fixture-tested contracts | 016 |
| Pages/OIDC privileges exist during dependency/build execution | 010 |
| GitHub Actions use mutable tags | 010 |
| Required full verification rewrites tracked deployment output | 010 |
| Tracked docs point to ignored/untracked agent instructions | 021 |
| Runtime declarations and ESLint core config are misaligned | 009 |
| Mobile CSS removes all sidebar actions without replacement | 020 |
| README mixes shipped capabilities with canonical aspiration | 021 |
| `App.jsx` mixes domain, external effects, orchestration, and UI | 022 |
| The SPA has no static type-check gate and its cross-domain contracts remain implicit | 027 |
| Typeahead recomputation is plausible but unprofiled | 023 |
| Quote Analyzer direction | 024 |
| Separate intent-first Guided and manual Expert Builders | 025 |
| Scenario comparison and upgrade decisions | 026 |
| Imported quotes still lack a versioned evidence-qualified verdict engine | 028 |
| Beachhead and identity-resolution coverage remain unmeasured on real Chilean quotes; supported rule reliability needs bounded automated assurance | 035 (supersedes rejected 029) |
| Catalog gates validate shape/volume but not rule-level assessability or runtime evidence | 030 |
| Acquisition, qualified activation, time-to-verdict, and decision actions have no privacy-governed event contract | 031 |
| The product still leads with a mislabeled builder and lacks confirmation-driven Analyzer UX | 032 |
| The production shell lacks Spanish/canonical/crawlable decision content and search-observation foundations | 033 |
| Chilean price intelligence lacks a qualified, permissioned aggregation source; SoloTodo is strategically aligned but its access, licensing, identity, freshness, and redistribution terms are unverified | 034 |
| Inexact schedule wording, unreachable pin-refresh help, stale archive rows | 021 |

### 2026-09-25 deep-audit finding coverage

Every net-positive finding from the 2026-09-25 deep audit (vetted against
`6cde4b8`, baseline `npm test` 942 passing / 27 todo) is assigned:

| Audit finding | Implementation plan |
|---|---|
| CI runs only on schedule/manual; artifact contracts not in a per-push gate | 037 |
| Two high dev/build toolchain advisories (`brace-expansion`, `nanoid`) | 037 |
| Plan 035 assurance CLI undocumented and unenforced | 037 |
| CI lacks dependency caching | 037 (steps 2-3) |
| Builder PSU power returns `ok` when CPU/GPU TDP is missing | 038 |
| Connector check returns `ok` for unparsed 6-pin/16-pin/unrecognized tokens | 038 |
| Compiler fabricates `recommended_psu_w: 148` for null-TDP GPUs | 038 |
| Blank quote rows counted as unpriced; measurement count inflated | 039 |
| Price CSV `id` column matched by substring ("Cantidad") | 039 |
| Inferred case form factor emits high-confidence critical finding | 039 |
| Analysis signature omits `quote.currency`/`quote.priceUpdatedAt` | 039 |
| 27 pending App tests including two callback-less `it()` | 040 |
| `exportCSV` tested with a local escaper instead of `escapeCsvField` | 040 |
| `builderReducer` API dead in production | 041 |
| Resolver rescans the full catalog per quote edit and while hidden | 042 |
| Candidate list unbounded before render | 042 |
| Raw parsed catalog retained after mapping (~40 MiB) | 042 |
| Linear ID lookups in resolver/session/selection paths | 042 |
| Analyzer purchase context write-once after first analysis | 043 |
| Compatibility-file failure swallowed; `assessmentCoverageFailed` unused | 043 |
| One nullish stored quote discards all quotes and overwrites storage | 044 |
| Two divergent import detectors; empty JSON reports success | 044 |
| Export filename from unvalidated quote name | 044 |
| `catalogMapper` throws on non-array form-factor field | 044 |
| `pushState` inside a `setState` updater (StrictMode duplicate history) | 044 |
| Stale design/validation docs; synthetic 30-quote report example | 045 |
| `scripts/` unlinted; lint config contradictions; warnings tolerated | 046 |
| Unreferenced hashed bundles under `docs/assets/` | 046 |
| Pipeline readers follow symlinks from pinned upstream checkouts | 047 |
| No product path to collect the private real-quote coverage corpus | 048 (design) |
| Missing rule-backed inventory for Milestone 1 pages 4-12 | 049 (design) |
| Silent destructive clears in the Expert builder | 050 |
| Measurement sink and `acquisitionClass` derivation undecided | 051 (design) |

## Archiving

Once a plan is DONE, move its file to `plans/archive/`, add completion commit/date/summary to the archived file, and remove its row from the active table in the same change. Do not renumber later plans.

## Findings already resolved

- CSV multiline parsing and quadratic price lookup — resolved by archived Plan 006.
- Dead root lockfile and parallel obsolete catalog fetch path — resolved by archived Plan 007.
- Initial all-at-once catalog fetching — partially resolved by archived Plan 008; remaining correctness/performance gaps resolved by Plan 019.
- Staged controls using undemanded fallback data — resolved by Plan 019 (categoryStates tracks per-category readiness; chip shows partial/fallback states).
- Reload token stuck in reload mode — resolved by Plan 019 (generationRef compares generations; isReload only fires on actual token change).
- In-flight catalog requests not deduplicated — resolved by Plan 019 (pending promise cache in dataLoader deduplicates concurrent same-URL calls).

## Considered and rejected or deliberately deferred

- **Hosted public quote sharing and generic automated price feeds**: hosted
  sharing remains deferred. A broad multi-provider feed remains beyond current
  scope; Plan 034 isolates SoloTodo as a permissioned feasibility candidate and
  does not authorize scraping or production ingestion.
- **Standalone catalog-confidence UI project**: not separately planned. Evidence/provenance behavior required for current correctness is embedded in Plans 014, 017-019, and the direction designs.
- **Professional/store CRM, branded exports, inventory integration**: deliberately long-term in the canonical vision; defer until the consumer decision engine is credible.
- **Blanket dependency upgrades, Prettier, pre-commit framework, search dependency, or virtualization**: no measured defect or security advisory justifies them. Plan 023 permits only measured, dependency-free optimization first.
- **Immediate deletion of `App.css`, React scaffold assets, `memory.min.json`, or unused `socketSet`**: cleanup value is too low or external artifact compatibility is uncertain; review opportunistically inside an owning future change.
- **Formal WCAG/accessibility overhaul**: not covered by the deep audit playbook and therefore not claimed as audited. Run the dedicated accessibility skill/audit separately before treating accessibility as complete.
- **Guided Builder production implementation**: remains gated on the shared
  Analyzer contract in production and Milestone 2 quality gates. Plan 025 is a
  design, not authorization to start Guided Phase B.
- **Recurring independent expert labeling or AI-as-expert substitution**:
  rejected as operationally infeasible and epistemically misleading. Plan 035
  provides bounded automated assurance; subjective gaming balance remains
  unsupported rather than receiving synthetic labels.
- **Scenario-comparison production UI**: remains downstream of Plan 032 even
  though Plan 026's design is complete. Do not build a totals-only Plan 026
  Phase A as a substitute for the primary Analyzer vertical.
- **Separate paste-ingestion roadmap item**: folded into Plan 032 as a bounded,
  structured-text adapter with explicit identity confirmation. OCR, screenshots,
  PDFs, URL scraping, and AI matching remain deferred.
- **Standalone Expert Builder rename/evidence project**: honest mode naming and
  Analyzer-to-Expert coexistence are included in Plan 032. Broader alternatives
  and non-destructive conflict work from Plan 025 should be planned after the
  Analyzer is stable.
- **Mass programmatic SEO or twelve pages immediately**: rejected. Plan 033
  establishes three authored rule-backed briefs and an observation gate before
  any expansion. Plan 049 prepares only the gated inventory.
- **No Content-Security-Policy / missing hardening headers**: rejected as a
  finding. The security audit found no HTML injection sink, no user/catalog
  data in `href`/`src`/HTML, and React escapes all rendered strings; the site
  is static GitHub Pages, so response headers are a hosting constraint rather
  than a live control.
- **CLP decimal price parsing** (`parsePrice("12990.00", "CLP")` → `1,299,000`):
  behavior is certain and noted by the audit, but whether spreadsheet-style CLP
  decimals are in-policy is a product call. Deferred pending an owner decision;
  plan 039 documents the deferral rather than changing money semantics.
- **Main-thread catalog JSON parse (~145 ms)**: measured but deferred as
  device-dependent; a Web Worker/chunked-artifact change is larger than the
  current evidence justifies. Plan 042 explicitly excludes it.
- **`AGENTS.md` stale plan table**: not deliverable through git — the file is
  gitignored and untracked, so a worktree executor cannot commit the fix.
  `plans/README.md` is the tracked status source; plan 045 records the manual
  sync note.
- **codegraph "no covering tests found" advisories**: rejected as false
  positives. The index's test-link pass does not resolve co-located Vitest
  files for several modules (e.g. `compatibility.test.js`, `session.test.js`),
  and the vetted findings rely on the executed suite, not on those advisories.
